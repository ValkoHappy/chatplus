---
import PageV2Page from './PageV2Page.astro';

interface Props {
  page: Record<string, any>;
}

const { page } = Astro.props as Props;
const previewPage = {
  ...page,
  force_v2_preview: true,
  robots: 'noindex,nofollow',
};
---

<PageV2Page page={previewPage} />
