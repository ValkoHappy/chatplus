---
import PageV2Page from '../../components/PageV2Page.astro';
import ResourceHubPage from '../../components/ResourceHubPage.astro';
import { getLandingPage, getManagedRoutePage } from '../../lib/strapi';

const resolved = await getManagedRoutePage('/docs', () => getLandingPage('docs'));
---

{resolved.kind === 'page_v2' ? <PageV2Page page={resolved.page} /> : <ResourceHubPage page={resolved.page} />}
